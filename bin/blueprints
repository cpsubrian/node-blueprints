#!/usr/bin/env node

/**
 * Blueprints
 *
 * A command-line utility for initializing project structures.
 */

var commander = require('commander');

commander
	.version('0.0.1')
	.parse(process.argv)

console.log("Sweet! Test commander interface working.");
